# Phase UAT: <PHASE_NAME>

## Status

`not_started`

Allowed status values:

- `not_started`
- `in_progress`
- `blocked`
- `passed`
- `failed`

## UAT objective

Describe what the user should be able to verify at the end of this phase.

Example:

> The user can run one command to scrape public V Point campaign cards and confirm that the extracted campaigns are stored without duplicates.

## Test environment

| Item | Value |
|---|---|
| OS | macOS / Windows / Linux |
| Python version |  |
| Browser | Chromium via Playwright |
| Database | SQLite |
| Test date |  |
| Tester |  |

## Preconditions

- [ ] Dependencies are installed.
- [ ] Playwright browsers are installed if needed.
- [ ] `.env` exists if needed.
- [ ] No private credentials are committed.
- [ ] Test database can be created safely.
- [ ] Network access is available if live scraping is tested.

## Test data

Describe test data or fixture files.

```text
tests/fixtures/
  sample_campaign_list.html
```

## UAT test cases

### UAT-001: Basic command runs

**Purpose**

Confirm the command runs without crashing.

**Steps**

```bash
python -m vpoint_scanner --help
```

**Expected result**

- Command help is displayed.
- No traceback occurs.

**Actual result**

- TODO

**Status**

`not_started`

---

### UAT-002: Scrape public campaign list

**Purpose**

Confirm campaign cards can be extracted from the target page.

**Steps**

```bash
python -m vpoint_scanner scrape --source vpoint_public
```

**Expected result**

- Scraper completes successfully.
- At least one campaign is extracted.
- Each campaign has a title.
- Raw text is preserved.
- Japanese text is not corrupted.

**Actual result**

- TODO

**Status**

`not_started`

---

### UAT-003: Deduplication

**Purpose**

Confirm running the scraper multiple times does not create duplicate campaigns.

**Steps**

```bash
python -m vpoint_scanner scrape --source vpoint_public
python -m vpoint_scanner scrape --source vpoint_public
python -m vpoint_scanner summary
```

**Expected result**

- Campaign count does not double after the second scrape.
- Existing campaigns have updated `last_seen_at`.
- Existing campaigns preserve `first_seen_at`.

**Actual result**

- TODO

**Status**

`not_started`

---

### UAT-004: JSON export

**Purpose**

Confirm campaigns can be exported to JSON.

**Steps**

```bash
python -m vpoint_scanner export --format json --output data/exports/campaigns.json
```

**Expected result**

- JSON file is created.
- JSON is valid UTF-8.
- Export contains `exported_at`.
- Export contains `campaign_count`.
- Export contains a `campaigns` array.
- Campaign titles remain in Japanese.

**Actual result**

- TODO

**Status**

`not_started`

---

### UAT-005: Safety check

**Purpose**

Confirm the app does not perform risky actions.

**Steps**

Review code and run normal scrape/export commands.

**Expected result**

- No automatic campaign entry.
- No automatic click on entry/application buttons.
- No password storage.
- No credit card application behavior.
- No spending recommendation generated by scraper.

**Actual result**

- TODO

**Status**

`not_started`

## Bug list

| Bug ID | Description | Severity | Status | Notes |
|---|---|---|---|---|
| BUG-001 |  |  |  |  |

## UAT result summary

### Final result

`not_started`

Allowed final values:

- `passed`
- `failed`
- `blocked`

### Summary

Write a short summary after testing.

Example:

> Public campaign scraping passed. JSON export passed. Deduplication passed. Date parsing still has limitations but raw date text is preserved, so this phase is acceptable.

## Sign-off checklist

- [ ] All must-have UAT cases passed.
- [ ] Known issues are documented.
- [ ] `.planning/STATE.md` was updated.
- [ ] Next phase recommendation is written.